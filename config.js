"use strict";

module.exports = {
    mysqlConfig: {
        host: "localhost",// Ordenador que ejecuta el SGBD
        user: "admin_aw", // Usuario que accede a la BD
        password: "", // Contraseña con la que se accede a la BD database: "tareas" // Nombre de la base de datos
        database: "viajes"
    },
    
    port: 3000
}